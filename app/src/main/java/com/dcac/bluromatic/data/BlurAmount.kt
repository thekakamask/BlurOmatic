package com.dcac.bluromatic.data

import androidx.annotation.StringRes

// CLASSE DE DONNÉES REPRÉSENTANT UN NIVEAU DE FLOU SÉLECTIONNABLE PAR L’UTILISATEUR
// CHAQUE INSTANCE CONTIENT :
// - UN TEXTE (RESOURCE STRING) POUR L’AFFICHAGE DANS L’INTERFACE
// - UNE VALEUR ENTIER UTILISÉE COMME NIVEAU DE FLOU DANS LE WORKER

data class BlurAmount(

    // ID D’UNE CHAÎNE DE CARACTÈRES DANS STRINGS.XML (ex: "Low", "Medium", "High")
    @StringRes val blurAmountRes: Int,

    // VALEUR ENTIER UTILISÉE POUR CONFIGURER L’INTENSITÉ DU FLOU
    val blurAmount: Int
)